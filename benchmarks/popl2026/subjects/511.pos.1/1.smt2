; Input: /benchmark/subjects/511.pos.1.py
(set-logic ALL)
(declare-const s String)
(assert (str.in_re s (re.++ (re.++ (re.opt (str.to_re "a")) (re.opt (str.to_re "b"))) (re.opt (str.to_re "c")))))
(assert (distinct s "abc"))
(assert (distinct s "ab"))
(assert (distinct s "a"))
(assert (distinct s "ac"))
(assert (distinct s "bc"))
(assert (distinct s "b"))
(assert (distinct s "c"))
(assert (not (= s "")))
(check-sat)
(exit)