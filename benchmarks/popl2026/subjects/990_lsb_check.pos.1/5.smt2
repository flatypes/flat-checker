; Input: /benchmark/subjects/990_lsb_check.pos.1.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (str.in_re s (re.++ (re.* (str.to_re "0")) (str.to_re "1"))))
(assert (>= i@1 (- (str.len s) 1)))
(assert (>= i@1 0))
(assert (<= i@1 (- (str.len s) 1)))
(assert (not (and (>= i@1 0) (< i@1 (str.len s)))))
(check-sat)
(exit)