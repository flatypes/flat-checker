; Input: /benchmark/subjects/490.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (let ((_let_1 (str.to_re "1"))) (let ((_let_2 (str.to_re "0"))) (str.in_re s (re.++ (re.++ (re.++ (re.* (re.union _let_2 _let_1)) _let_2) _let_1) _let_1)))))
(assert (< i@1 (str.len s)))
(assert (>= i@1 0))
(assert (<= i@1 (str.len s)))
(assert (>= i@1 0))
(assert (< i@1 (str.len s)))
(assert (>= i@1 0))
(assert (< i@1 (str.len s)))
(assert (let ((_let_1 (str.at s i@1))) (not (or (= _let_1 "0") (= _let_1 "1")))))
(check-sat)
(exit)