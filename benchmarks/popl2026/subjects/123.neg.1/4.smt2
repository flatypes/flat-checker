; Input: /benchmark/subjects/123.neg.1.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (let ((_let_1 (str.to_re "a"))) (str.in_re s (re.* (re.++ (re.diff re.allchar _let_1) (re.* _let_1))))))
(assert (< i@1 (str.len s)))
(assert (>= i@1 0))
(assert (<= i@1 (str.len s)))
(assert (>= i@1 0))
(assert (< i@1 (str.len s)))
(assert (not (or (> i@1 0) (= (str.at s i@1) "a"))))
(check-sat)
(exit)