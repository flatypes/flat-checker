; Input: /benchmark/subjects/470.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (let ((_let_1 (str.to_re "0"))) (let ((_let_2 (re.union _let_1 (re.++ _let_1 (str.to_re "1"))))) (str.in_re s (re.++ ((_ re.^ 0) _let_2) (re.* _let_2))))))
(assert (< i@1 (str.len s)))
(assert (<= 0 i@1))
(assert (<= i@1 (str.len s)))
(assert (let ((_let_1 (str.to_re "0"))) (let ((_let_2 (re.union _let_1 (re.++ _let_1 (str.to_re "1"))))) (str.in_re (str.substr s i@1 (- (str.len s) i@1)) (re.++ ((_ re.^ 0) _let_2) (re.* _let_2))))))
(assert (let ((_let_1 (+ i@1 1))) (or (>= _let_1 (str.len s)) (distinct (str.at s _let_1) "1"))))
(assert (let ((_let_1 (str.to_re "0"))) (let ((_let_2 (re.union _let_1 (re.++ _let_1 (str.to_re "1"))))) (let ((_let_3 (+ i@1 1))) (not (str.in_re (str.substr s _let_3 (- (str.len s) _let_3)) (re.++ ((_ re.^ 0) _let_2) (re.* _let_2))))))))
(check-sat)
(exit)