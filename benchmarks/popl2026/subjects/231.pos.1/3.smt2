; Input: /benchmark/subjects/231.pos.1.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (str.in_re s (re.++ (str.to_re "a") (re.* (str.to_re "b")))))
(assert (> i@1 0))
(assert (<= i@1 (str.len s)))
(assert (>= i@1 0))
(assert (distinct (str.at s (- i@1 1)) "b"))
(assert (>= (- i@1 1) 0))
(assert (< (- i@1 1) (str.len s)))
(assert (let ((_let_1 (- i@1 1))) (not (and (<= _let_1 (str.len s)) (>= _let_1 0)))))
(check-sat)
(exit)