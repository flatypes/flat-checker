; Input: /benchmark/subjects/530.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (let ((_let_1 (str.to_re "a"))) (str.in_re s (re.* (re.union (re.diff re.allchar _let_1) (re.++ _let_1 (str.to_re "b")))))))
(assert (< i@1 (str.len s)))
(assert (<= 0 i@1))
(assert (<= i@1 (str.len s)))
(assert (let ((_let_1 (str.to_re "a"))) (str.in_re (str.substr s i@1 (- (str.len s) i@1)) (re.* (re.union (re.diff re.allchar _let_1) (re.++ _let_1 (str.to_re "b")))))))
(assert (= (str.at s i@1) "a"))
(assert (>= i@1 0))
(assert (< i@1 (str.len s)))
(assert (let ((_let_1 (+ i@1 1))) (not (and (>= _let_1 0) (< _let_1 (str.len s))))))
(check-sat)
(exit)