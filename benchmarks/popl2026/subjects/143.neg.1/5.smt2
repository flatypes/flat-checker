; Input: /benchmark/subjects/143.neg.1.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (str.in_re s (re.* (re.diff re.allchar (str.to_re "a")))))
(assert (> i@1 0))
(assert (<= 0 i@1))
(assert (<= i@1 (str.len s)))
(assert (not (str.contains (str.substr s i@1 (- (str.len s) i@1)) "a")))
(assert (distinct (str.at s (- i@1 1)) "a"))
(assert (>= (- i@1 1) 0))
(assert (< (- i@1 1) (str.len s)))
(assert (let ((_let_1 (- i@1 1))) (not (not (str.contains (str.substr s _let_1 (- (str.len s) _let_1)) "a")))))
(check-sat)
(exit)