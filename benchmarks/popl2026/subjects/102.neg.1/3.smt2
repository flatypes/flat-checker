; Input: /benchmark/subjects/102.neg.1.py
(set-logic ALL)
(declare-const s String)
(declare-const i@1 Int)
(assert (let ((_let_1 (str.to_re "a"))) (let ((_let_2 (re.* (re.++ (re.diff re.allchar _let_1) (re.* _let_1))))) (str.in_re s (re.union (re.++ _let_1 (re.union (re.++ (re.++ _let_1 re.allchar) (re.* re.allchar)) _let_2)) _let_2)))))
(assert (< i@1 (str.len s)))
(assert (>= i@1 0))
(assert (<= i@1 (str.len s)))
(assert (>= i@1 0))
(assert (< i@1 (str.len s)))
(assert (not (= (str.at s i@1) "a")))
(check-sat)
(exit)