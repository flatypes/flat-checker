method parse(name: String): String
  requires !name.contains(":")
  ensures _ == name
{
  var colonIndex = name.indexOf(":");
  if (colonIndex < 0) {
    return name;
  } else {
    return "null";
  }
}