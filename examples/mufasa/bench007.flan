lang Stripped = r"[^# \n\r\t]*"

method stripCommentAndWhitespace(input: String): Stripped
{
  var line = input;
  var hash = line.indexOf("#");
  if (hash != -1) {
    line = line.substring(0, hash);
  }

  return line.trim();
}