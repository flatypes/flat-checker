const MAXLENGTH = 5

method truncateTag(tag: String): String
  ensures _.length <= MAXLENGTH
{
  if (tag.length > MAXLENGTH) {
    return tag.substring(0, MAXLENGTH - 1);
  } else {
    return tag;
  }
}